import React from 'react';
import { View, Text } from "react-native";

const SendIdea = ({navigation}) => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>의견 보내기</Text>
        </View>
    );
};

export default SendIdea;